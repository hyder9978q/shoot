# Design

## Theme

أخضر ملعب مشبع كهوية، أبيض نظيف كسطح عمل. Committed green على لحظات الهوية، Restrained داخل المهام.

## Colors

- `primary`: #16A34A (أخضر الملعب — أزرار أساسية، حالة نشطة)
- `primaryDark`: #0E7A38 (نهاية التدرجات، ضغط الزر)
- `primaryDeep`: #0B5D2B (أعمق نقطة بالتدرج)
- `primaryLight`: #DCFCE7 (خلفيات ناعمة للأيقونات والحالة)
- `ink`: #111827 (النصوص)
- `grey`: #4B5563 (نص ثانوي — يحقق 4.5:1 على الأبيض)
- `background`: #F6F8F7 (خلفية الصفحات — محايد بلمسة خضراء)
- `surface`: #FFFFFF (بطاقات ونماذج)
- `border`: #E5E7EB
- `accent`: #FACC15 (شارات وعروض — بجرعات صغيرة)
- `star`: #F59E0B (نجوم التقييم فقط — الأصفر ما يقرأ بحجم النجمة الصغير على الأبيض؛ ليلاً #FBBF24)
- `error`: #DC2626

تدرج الهوية: `primary → primaryDeep` قطري (رؤوس الصفحات، السبلاش).

## Typography

- عائلة واحدة: **Cairo** (400 / 600 / 700 / 800) — **مضمّنة محلياً** بـ `assets/fonts` (بدون google_fonts، تشتغل أوفلاين وتظهر بالاختبارات).
- المقياس: 12 / 13 / 15 / 17 / 20 / 26 — نسبة ~1.2.
- الأرقام والهواتف والأوقات دائماً `TextDirection.ltr`.

## Brand assets

- **الشعار (ShootMark)**: كرة قدم مرسومة بـ CustomPainter (خماسي مركزي + 5 رقع + درزات) مع خطوط سرعة صفراء اختيارية (`showTrail`) — `lib/core/widgets/shoot_logo.dart`. يُستخدم بالسبلاش وتسجيل الدخول ورأس الرئيسية.
- **أيقونة التطبيق (ShootAppIcon)**: نفس الشعار على تدرج الهوية مع دوائر ملعب خافتة. تُصدَّر PNG عبر `flutter test test/tools/app_icon_test.dart --update-goldens` ثم `dart run flutter_launcher_icons` (أندرويد تكيفية + iOS + ويب/favicon). المصدر: `assets/icon/`.
- **رسمة الملعب (FieldVisual)**: أرضية عشب متدرجة بأشرطة قص + خطوط الملعب حسب الرياضة (كرة قدم / سلة / تنس / بادل) — `lib/core/widgets/field_visual.dart`. الهوية البصرية لبطاقات الملاعب ورأس التفاصيل بدل الصور.

## Components

- **زر أساسي**: أخضر ممتلئ، ارتفاع 54، نصف قطر 16، ظل أخضر خفيف (12% blur 20 y8).
- **بطاقة**: بيضاء، نصف قطر 20، ظل أسود 5% blur 20 y6، بدون حدود.
- **بطاقة ملعب**: بانر `FieldVisual` ارتفاع 128 فوقه شارتا الرياضة والتقييم (أبيض 94%)، ثم الاسم + الموقع + السعر ودائرة سهم `primaryLight`.
- **رأس التفاصيل**: `FieldVisual` بارتفاع 210 مع تظليل علوي خفيف لوضوح زر الرجوع وشارة نوع الملعب بالأسفل.
- **رأس صفحة (هوية)**: تدرج أخضر، زوايا سفلية 28، المحتوى أبيض.
- **شيبس فلترة**: مستديرة 12، مفعّلة = أخضر ممتلئ نص أبيض؛ غير مفعّلة = أبيض بحد.
- **خانة إدخال**: بيضاء ممتلئة، نصف قطر 16، تركيز = حد أخضر 2px.
- **تبويبات سفلية**: 4 عناصر (الرئيسية، حجوزاتي، ناقصنا لاعب، حسابي)، مؤشر أخضر فاتح.

## Layout

- حواف صفحات 20px، فجوة بطاقات 12–14px، أقسام 24–28px.
- زوايا: 16 قياسي، 20 بطاقات، 28 رؤوس.

## Motion

- 150–250ms، ease-out. دخول السبلاش fade+scale؛ باقي الحركة حالة فقط.
